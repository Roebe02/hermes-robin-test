version: '3.8'

services:
  analyzer:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./repo_analyzer:/app
    command: uvicorn app:app --host 0.0.0.0 --port 8000 --reload
